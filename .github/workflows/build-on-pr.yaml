name: 'Build on Pull Request'
permissions:
    contents: read
on:
    pull_request:
        branches:
            - main

jobs:
    build:
        name: Build on Pull Request
        uses: sailpoint-oss/.github/.github/workflows/colab-saas-connectivity-pr.yml@v1

